ext {
    def sdkProperties = new Properties()
    sdkProperties.load(project.rootProject.file("sdk.properties").newDataInputStream())

    // google maps api
    wiliotApiKey = sdkProperties['wiliotApiKey']
}